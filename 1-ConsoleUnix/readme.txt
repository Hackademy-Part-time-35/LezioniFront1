nomeProgramma -r -f argomenti



pwd (print working directory)
clear


# directory
ls (list) listare il contenuto di una directory
cd (change directory)
cd pathDirectory
mkdir (make directory) crea una directory
rm (remove) (per cancellare una directory è necessaria l'opzione r recursive)

alias:
~ home dell'utente
. directory corrente
.. directory parent


# files
touch (crea un file)
cat stampa in output il contenuto di un file (lettura)


# vim (editor di testo)

2 modalità:
- command mode (insert -> command premere i tasti esc e poi :)
- insert mode (command -> insert premere il tasto i) in questa modalita posso scrivere nel mio file

:wq (write + quit)
:q! (quit forzato)